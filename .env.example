# ローカルでフロント(Vite等)と同一オリジン経由の動作確認をしたいときに使う。
# コメントアウトを外して、フロントのローカル開発サーバのポート番号を指定する。
# 未設定・コメントアウトのままでも/api/*経由のAPI疎通確認には影響しない。
# FRONTEND_PORT=8080

# APIへの振り分けパスプレフィックス（先頭のスラッシュは不要）。未設定時はapiになる。
# API_PATH=api
