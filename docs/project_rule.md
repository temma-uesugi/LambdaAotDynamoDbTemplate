# プロジェクト個別指示

## 必読ドキュメント

タスクを開始する前に、必ず `docs/spec.md`（仕様書）を読むこと。API仕様・DynamoDBテーブル設計・認証設計・Native AOT制約など、実装判断の前提となる情報が全てここにまとまっている。

## ビルド確認について

`dotnet build` 等のビルドコマンドを勝手に実行しないこと。Riderで編集中に外部からビルドされると、Rider側のキャッシュが壊れて再起動が必要になるため。コード修正後の動作確認が必要な場合は、ビルドを実行してよいかユーザーに確認する。
